$(document).ready(function() {

});

function loadExamples(event) {
  event.preventDefault();
  $('.content').load('hola');
}
function loadExcercises(event) {
  event.preventDefault();
  $('.content').load('hola');
}